<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined&display=block">
    <title>Dashboard Admin</title>
</head>

<body>
    <!-- Sidebar -->
    <div id="sidebar-container"></div>
    <!-- รวม sidebar และส่งข้อมูล admin ไปใน partial -->
    <%- include('../partials/sidebar', { user: admin }) %> <!-- ส่งข้อมูลผู้ใช้ admin สำหรับ Sidebar -->

    <div class="main-content">
        <div class="header">
            <span class="material-symbols-outlined">menu</span>
            <h5 class="mb-0">ข้อมูลคำขอที่ได้รับการอนุมัติ</h5>
            <span class="material-symbols-outlined">more_vert</span>
        </div>

        <!-- แสดงตารางข้อมูลคำขอที่อนุมัติ -->
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>รหัสคำขอ</th>
                    <th>ชื่อนักศึกษา</th>
                    <th>ชื่อบริษัท</th>
                    <th>ตำแหน่งงาน</th>
                    <th>ชื่อพี่เลี้ยง</th>
                    <th>วันที่อนุมัติ</th>
                </tr>
            </thead>
            <tbody>
                <% approvedRequests.forEach(function(request) { %>
                    <tr>
                        <td><%= request.request_id %></td>
                        <td><%= request.student_name %></td>
                        <td><%= request.company_name %></td>
                        <td><%= request.job_title %></td>
                        <td><%= request.mentor_name || 'ยังไม่มีพี่เลี้ยง' %></td> <!-- ตรวจสอบถ้ามีชื่อพี่เลี้ยง -->
                        <td><%= request.approved_date %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <br>
        <!-- ลิงก์ไปยังหน้าเพิ่มพี่เลี้ยง -->
        <a href="/admin/add-mentor" class="btn btn-primary">เพิ่มพี่เลี้ยงใหม่</a>

        <!-- Footer -->
        <div id="footer-container"></div>
        <!-- รวม footer -->
        <%- include('../partials/footer.html') %> <!-- รวมไฟล์ footer โดยไม่ใช้ .html ถ้ามี .ejs แทน -->

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>
