<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เพิ่มอาจารย์นิเทศให้กับนักศึกษา</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/css/select2.min.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            margin: 20px;
            background-color: #f9f9f9;
            text-align: center;
        }
        h3 {
            color: #000080;
        }
        .container {
            max-width: 800px;
            margin-top: 50px;
        }
        .card {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 60%;
            margin: 0 auto;
            text-align: left;
        }
        .select2-container--default .select2-selection--single {
            height: 38px;
            padding: 6px;
        }
        .profile-container div {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
        }
        .profile-container span {
            color: #555;
        }
        button {
            background-color: #000080;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
        }
        button:hover {
            background-color: rgba(0, 0, 128, 0.8);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h3 class="mb-3">เพิ่มอาจารย์นิเทศให้กับนักศึกษา</h3>
            <form>
                <div class="mb-3">
                    <label for="student" class="form-label">เลือกนักศึกษา</label>
                    <select id="student" class="form-select select2" multiple>
                        <% students.forEach(student => { %>
                            <option value="<%= student.user_id %>" data-id="<%= student.student_code %>">
                                <%= student.name %> (<%= student.student_code %>)
                            </option>
                        <% }); %>
                    </select>
                </div>

                <div class="mb-3">
                    <label for="mentor" class="form-label">เลือกอาจารย์นิเทศ</label>
                    <select id="mentor" class="form-select select2">
                        <% mentors.forEach(mentor => { %>
                            <option value="<%= mentor.user_id %>">
                                <%= mentor.name %>
                            </option>
                        <% }); %>
                    </select>
                </div>

                <button type="submit" class="btn btn-primary">บันทึกข้อมูล</button>
            </form>
        </div>
    </div>

    <!-- jQuery + Select2 -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/js/select2.min.js"></script>
    <script>
        $(document).ready(function() {
            $('.select2').select2({
                placeholder: "เลือกตัวเลือก",
                allowClear: true,
                matcher: function(params, data) {
                    if ($.trim(params.term) === '') {
                        return data;
                    }
                    if (data.text.toLowerCase().indexOf(params.term.toLowerCase()) > -1 ||
                        ($(data.element).data('id') && $(data.element).data('id').toLowerCase().indexOf(params.term.toLowerCase()) > -1)) {
                        return data;
                    }
                    return null;
                }
            });
        });
    </script>
</body>


</html>
