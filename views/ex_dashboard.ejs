<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ยินดีต้อนรับ ผู้ประเมิน</title>
    <!-- ลิงก์ไปยัง Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
        }

        .row {
            display: flex;
            justify-content: center; /* จัดการให้การ์ดอยู่กลาง */
            align-items: center;
        }

        .card {
            cursor: pointer;
            margin-bottom: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        }

        .card-body {
            text-align: center;
        }

        .card-title {
            font-size: 20px;
            font-weight: bold;
        }

        .card-text {
            color: #555;
        }

        /* ปรับขนาดและการจัดการกับ responsive */
        @media (max-width: 768px) {
            .card-title {
                font-size: 18px;
            }

            .card-text {
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="text-center mb-4">
            <h2>ยินดีต้อนรับ, <%= user.name %>!</h2>
            <p>กรุณาเลือกเมนูที่ต้องการใช้งาน</p>
        </div>

        <div class="row">
            <div class="col-md-4 col-sm-6">
                <div class="card shadow-sm" onclick="window.location.href='/ex_evaluator'">
                    <div class="card-body">
                        <h5 class="card-title">ประเมินการฝึกงาน</h5>
                        <p class="card-text">ประเมินการฝึกงานของนักศึกษา</p>
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="card shadow-sm" onclick="window.location.href='/exter_profile'">
                    <div class="card-body">
                        <h5 class="card-title">ดูข้อมูลส่วนตัว</h5>
                        <p class="card-text">ดูข้อมูลส่วนตัวของผู้ประเมิน</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- ลิงก์ไปยัง Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
